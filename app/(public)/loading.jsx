import PageSkeleton from "@/components/skeletons/PageSkeleton";

export default function Loading() {
  return (
    <PageSkeleton
      title="Opening Medicine App"
      description="Loading the application shell and preparing your workspace."
      variant="landing"
    />
  );
}
