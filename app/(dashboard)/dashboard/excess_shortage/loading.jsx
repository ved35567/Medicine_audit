import PageSkeleton from "@/components/skeletons/PageSkeleton";

export default function Loading() {
  return (
    <PageSkeleton
      title="Loading Excess & Shortage Report"
      description="Checking access and preparing download controls."
      variant="report"
    />
  );
}
